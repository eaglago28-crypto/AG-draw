#pragma once

namespace agdraw::ui {

enum class AlignMode {
    Left,
    HCenter,
    Right,
    Top,
    VCenter,
    Bottom,
};

enum class DistributeMode {
    Horizontal,
    Vertical,
};

} // namespace agdraw::ui
